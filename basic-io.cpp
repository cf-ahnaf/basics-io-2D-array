#include <iostream>
#include <array>
#define endl '\n'
using namespace std;
int main() {
  ios::sync_with_stdio(false);
  cin.tie(nullptr);
  // input
  // int row , col;
  // cin >> row >> col;
  // int arr[row][col];
  // for(int i = 0; i < row; ++i) {
  //   for(int j = 0; j < col; ++j) {
  //     cin >> arr[i][j];
  //   }
  // }
  // output
  // cout << "Your 2D array is ready!" << endl;
  // for(int i = 0; i < row; ++i) {
  //   for(int j = 0; j < col; ++j) {
  //     cout << "arr[" << i << "][" << j << "] : " << arr[i][j] << endl;
  //   }
  // }

  // Manual initialization

  // int A[4][2] = {
  //   {14 , 21},
  //   {12 , 18},
  //   {10 , 15},
  //   {6 , 9},
  // };
  // cout << "2D Array initialization : " << endl;
  // cout << "====================\n";
  // for(int i = 0; i < 4; ++i) {
  //   for(int j = 0; j < 2; ++j) {
  //     cout << "A[" << i << "][" << j << "] : " << A[i][j] << endl;
  //   }
  // }

  // int B[4][3]; // Declare the array

  // // Manually adding numbers
  // B[0][0] = 12;
  // B[0][1] = 25; 
  // B[0][2] = 38;

  // B[1][0] = 45;
  // B[1][1] = 52;
  // B[1][2] = 60;

  // B[2][0] = 78;
  // B[2][1] = 89;
  // B[2][2] = 90;

  // B[3][0] = 101;
  // B[3][1] = 112;
  // B[3][2] = 123;

  // cout << "2D Array manual initialization : " << endl;
  // cout << "====================\n";
  // for(int i = 0; i < 4; ++i) {
  //   for(int j = 0; j < 3; ++j) {
  //     cout << "B[" << i << "][" << j << "] : " << B[i][j] << endl;
  //   }
  // } 
  // cout << "========\n";
  // 2D array update data


  // int r , c , value; cin >> r >> c >> value;
  // cout << "Before Update Data B[" << r << "][" << c << "] : " << B[r][c] << endl;
  // B[r][c] = value;
  // cout << "After Update Data B[" << r << "][" << c << "] : " << B[r][c] << endl;
 
  
  return 0;
}